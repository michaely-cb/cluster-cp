apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cluster-server.fullname" . }}-test-connection"
  labels:
    {{- include "cluster-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cluster-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
