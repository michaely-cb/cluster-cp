!
! BGP config for ${name} to support multiple mgmt nodes
!
router bgp ${asn_num} vrf Vrf_${vrf_name}
    peer-group management_node
        remote-as ${asn_num}
        exit
    listen range ${prefix} peer-group management_node
    exit
