no interface vlan ${swarmx_vlan}
interface vlan ${swarmx_vlan}
   description "swarmx subnet"
   mtu ${mtu}
   vrf forwarding ${vrf_name}
   no ip address
   ip address ${swarmx_switch_address} primary
   exit

router bgp ${asn_num} vrf ${vrf_name}
  network ${swarmx_switch_prefix_sep}
