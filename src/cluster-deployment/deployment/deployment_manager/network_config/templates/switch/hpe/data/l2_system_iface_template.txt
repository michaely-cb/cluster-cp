#
# L2 config for ${my_port}
#
vlan ${switch_vlan}
exit

default interface ethernet ${my_port_num}
interface ethernet ${my_port_num}
    description L2 to ${neighbor}
    speed 100Gx4 no-autoneg force
    mtu ${mtu} force
    port link-mode route
    priority-flow-control enable
    opriority-flow-control no-drop dot1p 5
    exit
#
