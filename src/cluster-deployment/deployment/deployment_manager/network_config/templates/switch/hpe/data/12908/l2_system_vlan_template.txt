vlan ${system_vlan}
exit

interface Vlan-interface${system_vlan}
   description "system subnet"
   ip binding vpn-instance ${vrf_name}
   mtu ${mtu}
   ip address ${system_switch_address_netmask}
   no shutdown
   exit
#

bgp ${asn_num}
  router-id ${router_id}
  #
  ip vpn-instance ${vrf_name}
    address-family ipv4 unicast
      network ${system_switch_prefix_addr} ${system_switch_prefix_netmask}
      exit
    exit
  exit
