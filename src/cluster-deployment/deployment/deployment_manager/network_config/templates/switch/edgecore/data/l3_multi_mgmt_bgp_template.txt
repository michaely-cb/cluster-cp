#
# BGP config for ${name} to support multiple mgmt nodes
#
vtysh
configure
router bgp ${asn_num} vrf Vrf_${vrf_name}
    bgp listen range ${prefix} peer-group management_node
    exit
exit
write memory
exit
