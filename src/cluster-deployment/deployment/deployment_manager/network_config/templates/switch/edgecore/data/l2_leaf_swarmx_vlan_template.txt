sudo config vlan add ${swarmx_vlan}
sudo config interface vrf bind Vlan${swarmx_vlan} Vrf_${vrf_name}
sudo config interface ip add Vlan${swarmx_vlan} ${swarmx_switch_address}

vtysh
configure
router bgp ${asn_num} vrf Vrf_${vrf_name}
  network ${swarmx_switch_prefix}
  exit
exit
write memory
exit
