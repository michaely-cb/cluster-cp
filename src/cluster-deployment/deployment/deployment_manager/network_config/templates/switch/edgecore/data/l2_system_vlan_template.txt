sudo config vlan add ${system_vlan}
sudo config interface vrf bind Vlan${system_vlan} Vrf_${vrf_name}
sudo config interface ip add Vlan${system_vlan} ${system_switch_address}

vtysh
configure
router bgp ${asn_num} vrf Vrf_${vrf_name}
  network ${system_switch_prefix}
  exit
exit
write memory
exit
